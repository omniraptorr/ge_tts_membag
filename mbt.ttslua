require("MemBag/filterInstanceContainers")

local MemBag = require("MemBag")

function onLoad(save_state)
    local bagObj = --[[---@type tts__Container]] getObjectFromGUID("222a81")
    -- print(type(bagObj))
    -- print(bagObj ~= nil)
    local bag = MemBag(bagObj)
end
